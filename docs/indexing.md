INDEXING:

Document Result:

What changed? 
   The query runs faster after creating the index.


Why faster? 
   The database can quickly locate rows starting with 'T' without scanning the whole table.


Why indexing is important? 
  Indexes improve search performance, especially on large tables, by reducing the number of rows the database must examine.
